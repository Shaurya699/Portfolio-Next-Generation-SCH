import pageInfo from './pageInfo'
import skill from './skill'
import social from './social'
import project from './project'

export const schemaTypes = [pageInfo, skill, social, project]

types: schemaTypes.concat([
    pageInfo,
    skill,
    social,
    project,
]);